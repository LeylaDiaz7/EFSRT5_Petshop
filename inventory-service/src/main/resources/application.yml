server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petshop
    username: root


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
